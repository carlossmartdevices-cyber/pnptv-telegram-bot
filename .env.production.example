# This example file has been deprecated and cleared to avoid confusion.
# The canonical environment templates are now under:
#   /root/Bots/pnptv-payment/.env.example
#   /root/Bots/pnptv-payment/.env.production
# If you need to restore this template, please recreate it from the
# unified example in the pnptv-payment folder. Do NOT store secrets in
# repository files. Use Vercel environment variables for production.
